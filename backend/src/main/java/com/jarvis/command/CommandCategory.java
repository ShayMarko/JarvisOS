package com.jarvis.command;

/** Grouping used by the /help window (spec §5.1, field "category"). */
public enum CommandCategory {
    SYSTEM,
    FILES,
    AGENTS,
    MEMORY,
    WORKFLOWS,
    DEV,
    MONITORING,
    SECURITY
}
